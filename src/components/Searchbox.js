const Searchbox = ({ props }) => {
  return (
    <input
      className="searchbox"
      type="text"
      name="myCountry"
      placeholder="Enter city name..."
    ></input>
  );
};

export default Searchbox;
