import "./LoadingSpinner.css";
import { ReactComponent as SpinnerIcon } from "../../svg/spinner.svg";

const LoadingSpinner = () => {
  return <SpinnerIcon className="loading-spinner" />;
};

export default LoadingSpinner;
